# Use this file to add publications that cannot be fetched automatically
# or to override metadata (tldr, code links, etc.). Fields are optional.
# All fields are optional, and the file can be empty if no manual entries are needed.
